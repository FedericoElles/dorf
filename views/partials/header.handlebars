<amp-sidebar id="sidebar1" layout="nodisplay" side="left">
  <ul>
      <li><a href="/">Startseite</a></li>
    {{#each index.menu}}
      
      <li><a href="/article/{{slug}}">{{title}}</a></li>      
    {{/each}}    
  </ul>
</amp-sidebar>


<div class="u-font-special u-space-top-4 u-line-height-1 u-text-center c-title">
     
  <a href="/">{{index.title}}</a>

</div>

<hr class="c-hr c-hr--fading u-space-vert-4">


<!--
<div class="u-show@desk"> 
    {{#each index.menu}}
      {{#if isArticle}}
        <a href="/article/{{slug}}">{{title}}</a>      
      {{/if}}
      {{#if isPage}}
        <a href="/{{slug}}">{{title}}</a>      
      {{/if}}
    {{/each}}
</div>


<button class="hamburger u-show@mob" on='tap:sidebar1.toggle'>Menü</button>
<hr>
-->